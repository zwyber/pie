// Created by paul on 7/15/16.
//

#include "lib/game.h"

int main() {
    maingame();

    /*
    // Create a universe
    Universe universe;

    // Add an object
    Object* A = universe.add_object();

    // Set parameters
    A->set_position(3, 0);
    A->set_velocity(-2, 1);

    A->set_radius(2);
    A->set_mass(2);
    A->set_colour({1, 0, 0, 1}); // Red

    // Add another object
    Object* B = universe.add_object();
    B->set_position(-3, 0);
    B->set_velocity(2, -1);
    B->set_mass(1);
    B->set_radius(1);
    B->set_colour({0, 0, 1, 1}); // Blue

    // Create a window and bind the universe
    Window window;
    window.bindUniverse(&universe);

    // Loop the code below forever
    do {
        // Draw all objects in the universe
        window.drawObjectList();

        // Simulate one simulatinon time frame at 60 FPS
        universe.simulate_one_time_unit(60);

        // Pace the rendering so it matches 60 FPS
        window.pace_frame();

        // Swap buffers and clear the new one
        glfwSwapBuffers(window.GLFWpointer);
        glClear(GL_COLOR_BUFFER_BIT);
    }
    while (true);

     */







    return 0;
}