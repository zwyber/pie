#version 120

attribute vec2 inPosition;
attribute vec2 inTexcoord;

varying vec2 texCoord;

uniform mat3 projection;

void main()
{
    gl_Position = vec4(projection * vec3(inPosition,0.0) ,1.0);
    texCoord = inTexcoord;

}